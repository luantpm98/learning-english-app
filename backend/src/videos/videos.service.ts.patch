--- backend/src/videos/videos.service.ts
+++ backend/src/videos/videos.service.ts
@@ -73,9 +73,9 @@
       try {
         const videosData = await channel.getVideos();
         latestVideos = (videosData.videos || []).map((v: any) => ({
-          youtubeId: v.id,
-          title: v.title?.text || v.title?.toString() || '',
-          thumbnail: fixUrl(v.thumbnails?.[0]?.url),
+          youtubeId: v.id || v.content_id || v.videoId,
+          title: v.title?.text || v.title?.toString() || v.title || '',
+          thumbnail: fixUrl(v.thumbnails?.[0]?.url || v.image?.sources?.[0]?.url || v.thumbnail?.thumbnails?.[0]?.url || v.thumbnail?.url),
           duration: v.duration?.text || '',
           author: channel.metadata?.title || ''
         }));
